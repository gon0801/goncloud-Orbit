-- =============================================================================
--  ORBIT · MIGRACION 0013 · vista v_entidad_inerte (BIDS 01) · PostgreSQL 16
--
--  PLACEHOLDER del commit solo-tests (TDD, D-1.2.4): la vista real llega en
--  el commit de implementacion. Con este archivo vacio la CI debe fallar en
--  rojo (la vista no existe / la guarda no salta).
-- =============================================================================

-- (sin objetos todavia)
